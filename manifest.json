{
  "name": "aptaselect-viewer",
  "version": "1.4.1",
  "description": "AptaSelect pipeline dashboard: per-stage count trend + count-ranked table, plus a Motif tab for MEME results",
  "plugin_type": "pipeline",
  "pipeline_match": {
    "rules": [
      { "names": ["AptaSelect", "aptaselect", "aptaselect-v2", "aptaselect2", "aptaselect_test"] },
      { "required_files": ["survival_summary.tsv", "stage3_random_region.ranked.tsv"] },
      { "required_files": ["meme.xml"] }
    ]
  },
  "extensions": [],
  "entry": "index.js",
  "style": "style.css"
}
